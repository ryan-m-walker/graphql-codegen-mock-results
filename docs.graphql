query myQuery {
  pokedex {
    id
    created
    name
  }
}

query getPokemon($id: Int!) {
  pokemon(number: $id) {
    id
    abilities {
      edges {
        node {
          name
          description
        }
      }
    }
  }
}
